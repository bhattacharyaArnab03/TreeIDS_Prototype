# TreeIDS Prototype

This project builds a prototype system for transforming flat event logs into hierarchical tree structures and reasoning over them with an LLM-guided detection pipeline.

## Structure
- data/raw/: original CSV datasets
- data/processed/: extracted tree JSON structures
- src/: core Python modules
- outputs/: generated detection and visualization artifacts
